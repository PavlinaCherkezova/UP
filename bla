#include <iostream>
#include<cmath>

using namespace std;

int main()
{
    /*int x;

    cout<<"enter x= "; cin>>x;

    switch (x)
     {
         case (x<4): cout<<4*cos(x) + 2069; break;
         case (x==4): cout<<2016*pow(x,2); break;
         case (x>4): cout<<13*x + pow(cos(x),2); break;
     }
*/
/*
    double n, x, sum=0;
    cout<<"n= "; cin>>n;
    cout<<"x= "; cin>>x;

    for(double i=1; i<=n; i++)
    {
        sum+= sqrt(i) + pow((x/2), 2);
    }
    cout<<sum;

*/
    int n, a1= 1, a2=2;
    cout<<"n= "; cin>>n;

    if()
    for (int i=2

         ; i<=n; i++)
    {

    }


    return 0;
}
